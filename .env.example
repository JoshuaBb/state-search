# Copy to .env and adjust values
APP_DATABASE__URL=postgres://postgres:postgres@localhost:5432/state_search
APP_DATABASE__MAX_CONNECTIONS=10
APP_SERVER__HOST=0.0.0.0
APP_SERVER__PORT=3000

# sqlx offline mode (set DATABASE_URL for `cargo sqlx prepare`)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/state_search
